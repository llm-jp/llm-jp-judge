benchmark:
  ichikara:
    name: "ichikara"
    dataset: 
      path: null # For inference
      size: null # Use the first 'size' number of data entries
    metric: "abs_quality" # For evaluation
    sampling_params:
      max_tokens: 1024
      seed: 1234
      top_p: 0.95
      temperature: 1.0
      frequency_penalty: 0.0
  answer_carefully:
    name: "answer_carefully"
    dataset:
      path: null # For inference
      size: null # Use the first 'size' number of data entries
    use_reference: true # For evaluation
    metric: "abs_safety" # For evaluation
    sampling_params:
      max_tokens: 1024
      seed: 1234
      top_p: 0.95
      temperature: 1.0
      frequency_penalty: 0.0